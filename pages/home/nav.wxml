<template name="nav">
    <view class="nav" >
        <navigator wx:for="{{nav.items}}" wx:key="{{item.index}}"
         class=""
         url="{{item.url}}" open-type="redirect" > {{item.text}} </navigator> 
    </view>
</template>