<template name="nav">
    <view class="nav" >
        <navigator wx:for="{{items}}" wx:key="{{item.index}}"
         class="{{current==item.url ? 'current' : ''}}"
         url="{{item.url}}" open-type="redirect" > {{item.text}} </navigator> 
    </view>
</template>