<template name="nav">
    <view class="nav" >
        <navigator wx:for="{{items}}" wx:key="{{item.index}}"
         class="{{current==item.url ? 'current' : ''}}"
         url="index?page={{item.url}}"  > {{item.text}} </navigator> 
    </view>
</template>