<template name="nav">
    <view class="nav" >
        <navigator wx:for="{{nav.navItems.items}}" wx:key="{{item.index}}"
         class="nav.navItems.current"
         url="{{item.url}}" open-type="redirect" > {{item.text}} </navigator> 
    </view>
</template>